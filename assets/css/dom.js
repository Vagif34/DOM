const newUsername=document.querySelectorAll(".username");
const newEmail=document.querySelectorAll(".email")
const newPassword=document.querySelectorAll(".password")
const buttton=document.querySelectorAll(".btn")
document.getElementById("btn").addEventListener("click", printToScreen);


btn.forEach(item => {
    console.log(item);
    item.onclick =()=>{
        console.log();
    }
    
});
username.innertext
function printToScreen(){
        document.getinfo("").innertext=Info();
}